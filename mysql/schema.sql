-- CREATE USER 'andre'@'localhost' IDENTIFIED BY 'aNdr30516';
-- GRANT ALL PRIVILEGES ON * . * TO 'andre'@'localhost';
-- FLUSH PRIVILEGES;

CREATE DATABASE locadora;
USE locadora;

-- create table users( 
--   id int primary key auto_increment, 
--   nome varchar(30) not null 
-- );

-- create table users( id int primary key auto_increment, nome varchar(30) not null );
-- insert into users (nome) values ('Andre');
--
